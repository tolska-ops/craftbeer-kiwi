# craftbeer.kiwi - To-Do List

**Last updated:** 30 July 2026 (`TD-044` removed per Andy's request - not needed right now; retired, not reused, next new item is `TD-045` per ID convention; `TD-001` fixed and confirmed in prod - popup header-overlap and `GeolocateControl` positioning; Theme picker limited to Light/Dark - TD-023 dependency; DEV `is_published`/`has_theme` backfill drift found and fixed, `TD-043` added to audit for further gaps; Serial IDs, `TD-001` to `TD-043`, added to all outstanding items - see Purpose note below; "Get Directions" deep-link and distance-from-me items added; Beer Troopers/Beer Passport reference note added to Favourites item; Map/list view toggle and filterable feature badges ideas added from WOAP site review; Data drift buildable-soon items and Remote/cloud workflow migration to-do added; `region`/`ownership_type`/`has_taproom` columns added and backfilled)
**Purpose:** Single source of truth for what's outstanding on the project, so this doesn't only live in session memory. Update this whenever something here gets done, or something new gets added - same standing-instruction treatment as the other living docs (`craftbeer-kiwi-architecture.md`, `craftbeer-kiwi-automation-plan.md`, `craftbeer-kiwi-retrospective.md`). Brewery count lives in `craftbeer-kiwi-readme.md` — don't restate it here. **ID convention:** every outstanding item (Regressions through Not started) carries a permanent `TD-NNN` ID, assigned once in order of first appearance and never reused or renumbered - same pattern as `craftbeer-kiwi-decisions.md`'s `DEC-NNN` IDs. IDs stay with an item if it moves between sections; when an item is completed, its `TD-NNN` moves into the "Recently done" log entry (kept until that entry is trimmed) rather than being deleted immediately. New items get the next unused number, appended at the end of the current highest ID regardless of which section they land in.

Status key: 🔴 Blocked · 🟡 In progress / recommended, not started · ⚪ Not started · ✅ Done (kept briefly for context, remove once stale)

---

## Regressions

*(none currently outstanding — `TD-001` fixed and confirmed in prod 30 July, see "Recently done")*

## Hands-off readiness (new goal, logged 29 July)

**Why this section exists:** Andy's stated goal for the automation work isn't just "interesting to build" — it's specifically to get to a point where the project runs unattended while he's away (e.g. diving trips), without it becoming a burden to check in on. That's a sharper bar than "the functions work" — it means safe-to-run *and* actually-running *and* someone gets told if something breaks. None of the items below are hard, but they're a different kind of work from feature-building, and worth tracking as their own checklist rather than folding into general to-dos.

- ⚪ **TD-002 — Actually schedule `brewery-sync` and `brewery-discover`.** Both are still manual-trigger only. Dry-run-by-default (see `craftbeer-kiwi-decisions.md` DEC-024) makes scheduling *safe*, but safe-to-run and actually-running are two different things — this is the concrete next step toward the hands-off goal, not just a nice-to-have. `pg_cron` or Supabase's built-in scheduler, per the existing to-do below.
- ⚪ **TD-003 — Add alerting for scheduled-run failures.** Nothing currently notifies Andy if a scheduled run errors, if the Places API key hits a billing cap, or if anything else goes silently wrong. Needs a low-effort channel — email or a Slack webhook — wired into both Edge Functions before either goes on a schedule. Not yet decided which.
- ⚪ **TD-004 — Resolve the free-tier auto-pause conflict with "hands-off."** A week of no activity auto-pauses a free Supabase project. Low-stakes for `craftbeer-kiwi-DEV` (nobody depends on it), but worth checking whether production's real traffic level already prevents this, or whether a scheduled keep-alive ping is needed so a long trip doesn't risk the live site pausing.
- ⚪ **TD-005 — Confirm billing/renewal continuity while away.** Domain (Discount Domains), Vercel, Google Cloud, Supabase — worth a one-off check that everything's on auto-renew against a card that won't expire mid-trip. A lapsed domain is a bad thing to discover from overseas.
- ⚪ **TD-006 — Confirm account access survives losing a phone overseas.** 2FA is on for GitHub/Supabase/Vercel (done 25 July) — worth explicitly confirming backup codes are stored somewhere other than the phone itself, not just that 2FA is enabled.

## Data drift — buildable soon (discussed 30 July, full analysis in `craftbeer-kiwi-concept-data-drift.md`)

**Why this section exists:** a sanity check on automation surfaced that `brewery-sync`'s closure-check is genuinely monitored (runs, compares, flags), but almost everything else on a brewery's record — name, ownership, website content, branding — is a point-in-time snapshot with no re-check, including the `region`/`ownership_type`/`has_taproom` columns added this same session. These three items are the "cheap and structural" half of the fix; website content/branding monitoring is deliberately left out, flagged separately below.

- ⚪ **TD-007 — Relocation/new-venue fuzzy-match heuristic in `brewery-discover`.** When a candidate surfaces with a fuzzy name-match to an existing (including recently-closed) row within some radius, flag as "possible relocation or new venue, review before treating as new" rather than dropping into the ordinary new-candidate bucket. Already hit twice for real (Three Sisters New Plymouth→Wellington, Te Aro Brewing CBD→Brewtown) — both handled manually so far.
- ⚪ **TD-008 — Periodic `displayName`/`websiteUri` drift check.** Extend closure-check's Places pull beyond `businessStatus` on a periodic (not every-run, cost-conscious) sweep, comparing name/website against stored values and dropping any drift into an exceptions report — same detect-and-flag pattern as `has_theme`, never auto-overwrite.
- ⚪ **TD-009 — NZBN-backed `ownership_type` re-verification cadence.** Once the NZBN API integration is live (registration submitted 29 July, see above), add a scheduled quarterly pass re-checking legal status/ownership for existing rows, not just new ones — `ownership_type` is currently a single point-in-time snapshot (30 July).

**Flagged, not started, needs its own scoping session — not folded into the above:**
- 🔴 **TD-010 — Website content monitoring (description text, colour/branding).** No existing scaffolding at all — `description` is deliberately null, `getBreweryTheme` entries are hand-curated with no refresh mechanism. Realistic shape: a periodic Claude-API-assisted fetch-and-compare pass flagging "reads differently than last time" for human review, never auto-publishing. Genuinely different kind of work from the Places/NZBN-based automation above — a bad automated guess here (wrong colours, wrong description) is worse than a stale-but-known value, so this shouldn't be rushed into the pattern used for the other three items.

## Remote/cloud workflow migration (discussed 30 July, not started)

**Why this section exists:** Andy wants flexibility to work from a device other than his main machine, and to stop treating the C: drive as the single point of failure for docs and dev environment alike. Two separate work-streams — code/dev environment, and the docs master — with different urgency (code already has GitHub as a safety net; the docs master genuinely doesn't have an off-drive copy today).

- ⚪ **TD-011 — Confirm whether `tolska-ops` is a true GitHub Organization or a personal namespace.** Codespaces' free tier (120 core-hours/month, ~60 hrs on a 2-core machine) only applies to personal accounts — org-owned Codespaces bill from the first core-hour with no free allowance. Changes the cost calculus for the Codespaces plan below before committing to it.
- ⚪ **TD-012 — Set up a `devcontainer.json` for the repo** so Codespaces launches pre-configured (Node/Vite) rather than a generic container — removes the "reinstall everything" step each session.
- ⚪ **TD-013 — Move secrets into GitHub Codespaces' own secrets store** (Supabase keys, Google Places key) rather than relying on a local `.env.local` file, so a Codespaces session has what it needs without ever committing anything.
- ⚪ **TD-014 — Trial a real working session in Codespaces** (`npm install`, `npm run dev`, confirm hot-reload and the DEV Supabase project connects correctly) before relying on it for real work — don't switch cold.
- ⚪ **TD-015 — Move the docs master from `C:\craftbeer-kiwi\docs` into the existing Dropbox folder** (`Dropbox/Craftbeer-Kiwi/App Development/`), keeping it in a separate subfolder from the git repo so Dropbox's continuous sync never touches `.git` — sync clients and Git fighting over file locks is a known corruption risk.
- ⚪ **TD-016 — Update the standing memory/workflow note** that currently names `C:\craftbeer-kiwi\docs` as the local master, once the Dropbox move is confirmed working. Important to do this promptly — two competing "masters" existing at once is the same drift risk that caused the project-knowledge duplicate-file issue (and the lost `DEC-025`) already.
- ⚪ **TD-017 — Point the existing FreeFileSync → SanDisk USB backup at the new Dropbox-synced location** once docs move, so the offline backup keeps covering the actual master rather than a now-stale local folder.
- ⚪ **TD-018 — Enable 2FA on the Dropbox account itself.** It becomes a second live copy of the docs once it's the sync target, not just a PDF drop box — worth the same account-security treatment already given to GitHub/Supabase/Vercel.
- ⚪ **TD-019 — Test access from a second device** (phone or another PC) to confirm the actual point of this — real flexibility — before considering the migration done.

## In progress / recommended, not yet actioned

- 🟡 **TD-020 — Take a manual database backup.** Confirmed 29 July: Supabase Free tier has no automated backups or PITR — a deleted project (not just a paused one) is unrecoverable. Mitigation is a manual `supabase db dump` export, saved into the existing FreeFileSync-backed local folder. No automation needed at 23 breweries — see `craftbeer-kiwi-security.md` "Data backups" for the command and trigger cadence. Worth doing once now as a baseline.

- 🟡 **TD-021 — Add real `getBreweryTheme` entries for the 4 breweries added via `brewery-discover` (27 July).** Three Sisters, Double Vision - DUB HUB Island Bay, Panhead Tory Street, and Waitoa Victoria St. Draft theme entries were prepared and committed to `App.jsx`, but this needs a verification pass to confirm they're real, correct entries and not placeholders. Now backed by a structural safeguard either way: the `is_published` check constraint (added 28 July, see `craftbeer-kiwi-decisions.md`) makes it impossible for a brewery row to be published without `has_theme = true`, so a missing entry will show up in the exceptions report rather than silently going live.
- 🟡 **TD-022 — Clear stale `flagged_for_review` flags** on the 4 confirmed breweries from the 27 July discovery run, now that they're verified. Garage Project Aro Taproom no longer part of this — it was confirmed a genuine second site (not a duplicate) and reactivated 31 July, see `DEC-015` correction.
- 🟡 **TD-023 — Dive Bar / Hop Explosion theme visuals.** Structurally wired up but still on placeholder Mapbox style URLs. Two paths: (1) Mapbox's own Standard style with Monochrome/Faded presets - architecturally different from the current classic `light-v11`/`dark-v11` styles; (2) build fully custom styles in Mapbox Studio from scratch. Andy leaning towards (2) as a learning exercise.
- 🟡 **TD-024 — Garage Project NZBN - unresolved.** All four Garage Project rows (Aro Street, Aro Taproom, Leeds Street, Wild Workshop) traced to Brewwell Holdings Limited, but the actual operating entity is very likely a sibling "Brewwell Limited" that wasn't confirmed. Left `nzbn` blank and `on_watchlist = true` rather than guess. Aro Taproom added to the watchlist 31 July (it missed the original 28 July backfill pass because it was inactive at the time, not because its situation differs). Andy's call whether to chase this down or leave it watchlisted.
- 🟡 **TD-025 — Craft-curation product question, not yet decided - now with a second example.** Panhead (Lion/Kirin-owned) and Tuatara (DB/Heineken-owned) are corporately owned by international brewers while retaining independent branding - surfaced during the 28 July NZBN backfill. Sprig + Fern (Nelson dry run, same day) adds a different angle: independently owned (confirmed via NZBN, no corporate parent) but grown into a multi-region pub chain from a single Richmond brewery. Whether the site's "craft" framing should treat either case differently is still an open product question, not a decision - both findings logged in `craftbeer-kiwi-decisions.md`.
- 🟡 **TD-026 — NZBN API integration — registration submitted 29 July.** Account created at api.business.govt.nz, subscribed to NZBN – v5 via subscription-key method (production + sandbox keys requested together). State "Submitted", awaiting MBIE approval. Once approved: wire sandbox key into `craftbeer-kiwi-DEV` first, then `brewery-sync` can upgrade from single-source `flagged_for_review` writes to real two-source auto-close. Note the manual 28 July backfill (see `craftbeer-kiwi-decisions.md`) confirmed name-matching will still need judgment calls even once live — this closes the *access* gap, not the fuzzy-matching one.

## Not started

- ⚪ **TD-027 — Mapbox GL symbol-layer / label-collision migration.** Longer-term: if marker density grows further, migrate from the current hybrid pin/label approach to Mapbox GL's symbol-layer system for pins too, giving native collision detection for pins as well as labels. Known limitation confirmed during zoom-label testing: since pins are separate React `Marker` components outside Mapbox's own rendering, a label can still visually overlap a *neighbouring pin* even though it correctly avoids overlapping other labels.
- ⚪ **TD-028 — Anthropic API description-generation step.** Auto-draft a 1-2 sentence description for newly-discovered breweries. Likely lands inside `brewery-discover`, not decided for certain.
- ⚪ **TD-029 — Schedule the Edge Functions** (`pg_cron` or Supabase's built-in cron). Both have a proven successful manual run, but hold off scheduling `brewery-discover` specifically until `dryRun` exists - see above.
- ⚪ **TD-030 — Favourites / brewery trail persistence without user accounts.** Design discussed 21 July: browser-generated `crypto.randomUUID()` in `localStorage`, a `trails` table keyed to that ID, a scheduled Edge Function deleting rows older than 7 days, a separate public share-code for sharing. Nothing built yet. **Reference (30 July):** Beer Troopers' "Beer Passport" (South Africa) is the closest real-world analog found so far - a map-based tool for tracking visited independent breweries with a passport/collection mechanic. Worth a proper look at their approach when this feature actually gets scoped.
- ⚪ **TD-031 — Name search feature.** Client-side filtering of the in-memory breweries array by name, applied before the supercluster index so results recluster correctly. Likely its own `SearchBar.jsx` component.
- ⚪ **TD-032 — "Get Directions" deep-link button.** Popup button linking out to the visitor's own maps app (`https://www.google.com/maps/dir/?api=1&destination={lat},{lng}`, or `maps://` on iOS) using the brewery's stored coordinates. Deliberately not building in-app Mapbox Directions API routing - that's meaningfully more work (API calls, route rendering, per-request cost) for a worse result than handing off to Google/Apple Maps, which already have live traffic and turn-by-turn. Low effort, no new dependencies or cost.
- ⚪ **TD-033 — "Distance from me" readout on popup/card.** Client-side haversine calculation against the visitor's existing geolocation (already captured for the user-location marker) - e.g. "2.3 km away" - to help visitors unfamiliar with Wellington judge how far a brewery actually is. No new API needed.
- ⚪ **TD-034 — Map/list view toggle.** Spotted on Visa Wellington On a Plate's explore page (30 July) - a "VIEW MAP" / "HIDE MAP" button switches between the map and a scrollable list rendering the same result set. We've got the map; a list-view fallback could be a low-effort addition, especially useful for mobile users who'd rather scroll a list than tap individual pins. No design work done yet - would need a list-item component (reusing popup-card content) and a view-state toggle in `App.jsx`.
- ⚪ **TD-035 — Filterable feature badges (speculative, likely too-hard-basket for now).** Also spotted on WOAP's explore page (30 July) - small inline tags per card (GF, NF, V, VV, H, "beer match") acting as both a visual badge and a filter. Same pattern as our existing `venue_type`/`has_theme` badges. Only worth pursuing once `has_taproom`/`ownership_type` (added 30 July) or a low-alcohol flag turn into actual filterable attributes - right now we don't have enough badge-worthy, stable, filterable facts per brewery to justify the UI work. Flagged for reference, not scoped.
- ⚪ **TD-036 — Google Cloud API key IP restriction.** Deferred until the Edge Functions' egress IPs are known.
- ⚪ **TD-037 — Multi-country domain/branding strategy.** Deliberately parked until Wellington is proven and national NZ coverage is actively underway.
- ⚪ **TD-038 — Brewery popularity / heat-map view.** Idea logged 29 July, inspired by AllTrails' busyness heat-maps: once real Vercel Analytics data exists, surface which breweries get the most clicks/visits on the map itself (e.g. shading or sizing pins by relative popularity), rather than treating every pin as equal. Needs enough real traffic data to be meaningful first — not actionable yet, but worth keeping in mind as an Analytics-dependent feature rather than forgetting the idea. See `craftbeer-kiwi-concept-benchmarks.md`.
- ⚪ **TD-039 — Distribution plan.** Domain is live and Analytics is on, but still no plan actioned yet. One candidate idea logged 29 July, inspired by TapHunter's embeddable JSON tap-list feed: an embeddable widget/badge (e.g. a small "we're on craftbeer.kiwi ↗" static map pin or iframe) a brewery could place on their own site or social bio — turning existing brewery relationships into a distribution channel rather than a one-way listing. Would need individual brewery pages/routing to exist first (not yet built). Needs a proper scoping session, not a quick add.
- ⚪ **TD-040 — Vercel Hobby plan commercial-use terms check.** Worth confirming Hobby's "personal, non-commercial" terms against the fact the site is now owned by a registered company, before any future monetisation.
- ⚪ **TD-041 — MoJ register lookup.** Second Phase 2 verification source alongside NZBN - open question, not started.
- ⚪ **TD-042 — Discount Domains portal bug** - worth a documentation note to future-self, no action needed (resolved via phone support).
- ⚪ **TD-043 — Audit `craftbeer-kiwi-DEV` for other silently-missed backfills.** Second confirmed instance of the same drift pattern (30 July): `is_published`/`has_theme` were never backfilled to DEV's active test breweries when added 28 July, causing them to silently fail the frontend's publish-gate filter and vanish from the local map with no error - found only because Andy noticed the map was empty. First instance was `region`/`ownership_type`/`has_taproom`. Worth a one-off column-by-column comparison between DEV and prod schemas/data to check for other quiet gaps, rather than waiting to trip over each one individually.

---

## Recently done (context only - safe to trim once this feels stale)

- ✅ **`description` backfilled for 5 rows missing it, Aro Taproom added to NZBN watchlist** (31 July). Andy noticed some breweries had no description; a full null-content audit across all columns found 5 of 24 active rows missing `description` (Double Vision - DUB HUB Island Bay, Garage Project Aro Taproom, Panhead Tory Street, Three Sisters Brewery Ltd, Waitoa Victoria St) - everything else was fully populated. Written via verified web search, matching the existing one-sentence style. Also added Aro Taproom to the Garage Project NZBN watchlist note it had missed while inactive (see `TD-024`). Full writeup in `craftbeer-kiwi-decisions.md` (`DEC-030`).
- ✅ **Garage Project Aro Taproom reactivated — was wrongly suppressed as a duplicate** (31 July). The 27 July `DEC-015` call treated it as a near-duplicate listing of Garage Project and suppressed it via `is_active = false`. Address check showed this was wrong — Garage Project (68 Aro Street) and Aro Taproom (91 Aro Street) are two different premises on the same street. Web search confirmed Aro Taproom is Garage Project's own currently-trading taproom, across the road from the brewery. Reactivated (`is_active = true`); `is_published`/`has_theme` were already `true` so no other change needed. Live brewery count returns to 24. Full writeup in `craftbeer-kiwi-decisions.md` (`DEC-015`, corrected).
- ✅ **`TD-001` — Mobile popup header-obscuring bug fixed for real, and `GeolocateControl` button unblocked** (30 July). Header-obscuring fix: added a `headerRef` and read `headerRef.current?.offsetHeight` at click time instead of the hardcoded `top: 120` padding value, so the popup's fly-to offset now tracks the header's actual current height rather than a number tuned before the 21 July theme-dropdown addition made the header taller - self-correcting against any future header height change, not just this one. While testing, found a second, previously-unnoticed instance of the same root cause: Mapbox's own `GeolocateControl` button (`position="top-right"`) was rendering underneath the header too, making it unreachable/unclickable - fixed by moving it to `position="bottom-right"`, clear of the header regardless of height. Both confirmed working in production (real pin clicks, real geolocate button click centring correctly) same session. Full writeup in `craftbeer-kiwi-decisions.md` (`DEC-029`).
- ✅ **Theme picker hidden to Light/Dark only, and DEV `is_published`/`has_theme` drift fixed** (30 July). `App.jsx`'s theme `<select>` now filters against a new `VISIBLE_THEME_IDS = ['light', 'dark']` constant rather than listing all of `THEMES` - Dive Bar and Hop Explosion stay fully defined in the registry (nothing lost) but no longer appear in the dropdown until their real Mapbox style URLs exist (`TD-023`). While testing this against `craftbeer-kiwi-DEV`, found the map showing zero pins with no error - traced to `is_published`/`has_theme` (added 28 July) never having been backfilled to DEV's two active test breweries, so they silently failed the frontend's publish-gate filter. Fixed via direct SQL update (`is_published = true, has_theme = true` on Test Brewery Alpha and Beta) - the `theme_required_to_publish` constraint required both columns set together. Second confirmed instance of DEV/prod backfill drift - see `TD-043`. Full writeup in `craftbeer-kiwi-decisions.md` (`DEC-028`).
- ✅ **`region`, `ownership_type`, `has_taproom` columns added and backfilled** (30 July). Built in `craftbeer-kiwi-DEV` first, then production. `region` sourced from a Brewers Guild of NZ member-listing Andy supplied — all 24 brewery rows and 7 bar rows set to `'Wellington, Wairarapa & Manawatū'`. `ownership_type` (`independent`/`corporate-subsidiary`) reuses the 28 July NZBN backfill findings — Panhead ×2 and Tuatara are the three corporate-subsidiary rows. `has_taproom` checked individually via web search for every brewery — all 24 confirmed `true` (nothing production/distribution-only currently in the directory). All three nullable, no default guess for future automated inserts. **Note:** this resolves `automation-plan.md`'s `AP-PREREQ-3` (region boundary definition) — `automation-plan.md` updated to reflect this 30 July, alongside adding permanent `AP-PREREQ-N` IDs to all three prerequisites. Full writeup in `craftbeer-kiwi-decisions.md` (`DEC-026`), which also flags a `DEC-025` numbering gap worth checking against the local master file.
- ✅ **Regional dry-run pagination added, and the Thorndon/Petone question resolved** (28 July). A Nelson region dry run (first test of the new `?query=`/`?lat=`/`?lng=`/`?radius=` overrides, ahead of national expansion) surfaced Sprig + Fern as a known national pub chain with non-brewing satellite locations - prompting a check of whether Wellington's own Sprig + Fern pubs (Thorndon, Petone) were being missed due to the 20-result-per-request cap. Added pagination via `nextPageToken` (`MAX_PAGES = 4`, up to 80 candidates per run) to test this properly. Re-running Wellington confirmed `found` stayed at exactly 20 either way - not a volume problem. Real explanation: Thorndon and Petone are pubs that pour Sprig + Fern beer, not breweries, so Google's relevance ranking correctly doesn't return them for a "brewery" query - same pattern as Nelson's own non-Richmond Sprig + Fern locations. Pagination kept anyway (genuinely useful for larger regions, especially Auckland) even though it wasn't the fix for this specific case. Full writeup in `craftbeer-kiwi-decisions.md` (DEC-023).
- ✅ **Nelson region dry run - first regional test ahead of national expansion** (28 July). Confirmed working: 20 candidates found, correctly split into 5 genuine breweries (McCashins, Acoustic, Totara, Sprig + Fern Richmond, Eddyline Stoke) and 15 correctly-typed non-breweries (pubs, bars, restaurants, a museum, a bottle shop) via `primary_type`. Surfaced Sprig + Fern as independently-owned (confirmed via NZBN `9429034586907`, no corporate parent) but operating as a multi-region pub chain from a single Richmond brewery - added as a second, distinct angle on the open craft-curation question (see "In progress" above).
- ✅ **`dryRun` flag built for `brewery-discover`** (28 July). `?dryRun=true` returned `wouldInsert`/`skippedNames` without writing to `breweries`. **Correction (29 July):** this was opt-in only — the underlying "dry-run should be the default" decision (see below) hadn't actually shipped, despite being logged as done. Caught during the first security audit by checking the deployed code directly, not just the docs.
- ✅ **`brewery-discover` dry-run made the actual default, and legacy Supabase key disabled** (29 July, first security audit). Two real findings from the audit: (1) a legacy `service_role` key that bypasses RLS was still active though unused — disabled via Supabase's "Disable JWT-based API keys." (2) `dryRun` was still reading `?dryRun=true` as opt-in, not defaulting to safe — fixed to `const forceLive = url.searchParams.get("live") === "true"; const dryRun = !forceLive;`, deployed, and verified live: an unparameterised call correctly returned `dryRun: true, inserted: 0`. Full findings in `craftbeer-kiwi-security.md` audit log.
- ✅ **Search-query narrowing tried and reverted** (28 July). Tested filtering `brewery-discover`'s Places search with `includedType: "brewery"` + `strictTypeFiltering: true` (Google's new Feb 2026 brewery type category), using `dryRun` to check results before trusting it live. Found it unreliable: only matched venues with "Brewery"/"Brewer" literally in their own name, missing 15 of 23 known breweries entirely (Panhead, Boneface, Three Sisters included). Reverted to the original broad query. Kept `primaryType`/`primary_type` (new column, DEV + prod) as triage metadata only, not a filter - correctly flagged "Teddy's Tacos: Taproom & Taqueria" as `primary_type: "restaurant"` on the very first test after reverting, confirmed by Andy as a bar/taproom, not a brewery. Full writeup in `craftbeer-kiwi-decisions.md` (DEC-022).
- ✅ **NZBN backfill across all 23 breweries** (28 July). Manually looked up and recorded `nzbn` + `nzbn_entity_name` for every brewery (bars excluded). Confirmed two genuine multi-site matches (Waitoa, Double Vision); found the "shared NZBN = same brand" theory breaks down for conglomerate-owned brands (Panhead/Lion, Tuatara/DB). Full findings in `craftbeer-kiwi-decisions.md`.
- ✅ **`is_published` flag + publish-gate constraint** (28 July). New `is_published` column plus a check constraint that makes it structurally impossible to publish a brewery without an explicit theme entry. All 23 existing breweries defaulted to published with zero constraint violations - nothing changed on the live map.
- ✅ **National expansion plan decided** (28 July). Region-by-region rollout, not a single national pass - Auckland and Canterbury first, then Waikato/Bay of Plenty/Otago, then the rest batched. `dryRun` is now a hard blocker on starting. Full plan in `craftbeer-kiwi-automation-plan.md`.
- ✅ **`on_watchlist` / `watchlist_note` fields added** (28 July). Internal-only pair for unconfirmed signals worth a human's attention but not yet actionable (e.g. Boneface's NZBN showing "In Liquidation," likely a stale pre-ownership-change record).
- ✅ **Supabase secret-key 401 issue resolved** (27 July) - not a platform bug. `@supabase/server`'s secret-key auth mode needs a *named* key (`auth: "secret:<name>"`); both Edge Functions had just `auth: "secret"`. Fixed by referencing the named key (`brewery_sync_v2`) and redeploying.
- ✅ **`brewery-discover` first successful end-to-end run** (27 July). 12 candidates found: 7 were bars/pubs (reclassified via `venue_type`), 4 were genuine new breweries (Three Sisters, plus second sites for Double Vision, Panhead, and Waitoa), 1 was a near-duplicate Garage Project re-listing (suppressed via `is_active = false`). Brewery count moves from 19 to 23.
- ✅ **Geolocation fallback bug fixed** (27 July). Both failure paths (`!navigator.geolocation` and the `getCurrentPosition` error callback) previously fell back to a hardcoded Wellington CBD coordinate; now they leave `userLocation` as `null`, so the marker simply doesn't render rather than showing a misleading fake position. Also switched to `{ enableHighAccuracy: true, maximumAge: 0, timeout: 10000 }` to reduce the chance of a stale/cached low-accuracy fix being used. Directly addresses the 26 July Blenheim visitor report. Confirmed 28 July.
- ✅ **Zoom-dependent brewery name labels** (27 July). Native Mapbox symbol layer (`<Source>`/`<Layer>`) renders brewery names once zoomed in past `minzoom={14}` - chosen because it lines up with the existing pin-click `flyTo` zoom level, so labels tend to appear right around where users naturally land. Built on top of the existing pin/clustering system without changing it. Confirmed 28 July.
- ✅ **`venue_type` field added** (27 July). New column on `breweries` (`'brewery'`/`'bar'`), tested first in `craftbeer-kiwi-DEV`. Frontend query filters `venue_type = 'brewery'`. Deliberately a string field to leave room for a possible future pubs & bars layer.
- ✅ **Dev/prod Supabase environments split** (27 July). Second free Supabase project (`craftbeer-kiwi-DEV`, Tokyo region) - already caught a schema mix-up and real bugs before they reached production. `venue_type`/`has_theme` backfilled to DEV.
- ✅ **craftbeer.kiwi domain pointed at Vercel** (27 July).
- ✅ **Vercel Web Analytics installed and deployed** (27 July).
- ✅ 2FA enabled on GitHub, Supabase, and Vercel accounts (25 July).
- ✅ Garage Project Wild Workshop added as brewery entry #19, committed 25 July.
- ✅ README.md brewery count corrected - now shows 23.
